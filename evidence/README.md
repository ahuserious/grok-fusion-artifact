# Evidence

`campaign-summary.json` indexes the campaign. `runs/` contains the exact safe direct-xAI replay. `preflight/` preserves safe metadata from two failed attempts. `native-build/` contains a curated receipt and hash commitments rather than private session contents.

The exclusion policy is explicit in `SECURITY.md` and `LIMITATIONS.md`; omitted private bytes are not represented as published.
